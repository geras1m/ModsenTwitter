import { LoginForm } from '@/components/LoginForm';
import { LoginPageWrapper } from '@/pages/LogInPage/styled';

export const LogInPage = () => (
  <LoginPageWrapper>
    <LoginForm />
  </LoginPageWrapper>
);
